{
  "name": "3d-viewer",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "electrobun": "latest",
    "three": "^0.172.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/three": "^0.172.0"
  },
  "scripts": {
    "start": "bun run build:dev && electrobun dev",
    "build:dev": "bun install && electrobun build"
  }
}
